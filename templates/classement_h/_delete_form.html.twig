<form method="post" action="{{ path('app_classement_h_delete', {'id': classement_h.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classement_h.id) }}">
    <button class="btn">Delete</button>
</form>
