{% extends 'base.html.twig' %}

{% block title %}ClassementH{% endblock %}

{% block body %}
    <h1>ClassementH</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classement_h.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classement_h.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ classement_h.description }}</td>
            </tr>
            <tr>
                <th>BasePrice</th>
                <td>{{ classement_h.basePrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classement_h_index') }}">back to list</a>

    <a href="{{ path('app_classement_h_edit', {'id': classement_h.id}) }}">edit</a>

    {{ include('classement_h/_delete_form.html.twig') }}
{% endblock %}
